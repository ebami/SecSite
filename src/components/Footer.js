import React from 'react';
import config from '../../config';
export default function Footer() {
  return (
    <section id="footer">
      <div className="inner">
        <h2 className="major">Get in touch</h2>
        <p>
          For any enquires use the contact form or contact via the email address.
        </p>
        <form method="post" action="/#">
          <div className="fields">
            <div className="field">
              <label htmlFor="name">Name</label>
              <input type="text" name="name" id="name" />
            </div>
            <div className="field">
              <label htmlFor="email">Email</label>
              <input type="email" name="email" id="email" />
            </div>
            <div className="field">
              <label htmlFor="message">Message</label>
              <textarea name="message" id="message" rows="3"></textarea>
            </div>
          </div>
          <ul className="actions">
            <li>
              <input type="submit" value="Send Message" />
            </li>
          </ul>
        </form>
        <ul className="contact">
          {config.address ? <li className="fa-home">{config.address}</li> : ""}

          {config.phone ? <li className="fa-phone">{config.phone}</li> : ""}

          {config.socialLinks.map(social => {
            const { icon, url } = social;
            return (
              <li className={`${icon}`} key={url}>
                <a href={url}>{url}</a>
              </li>
            );
          })}
        <a target="_parent" href="https://www.addtoevent.co.uk/4t-knox-secure"><img className="event" src="https://www.addtoevent.co.uk/sites/default/files/badges/featured_circular_white_large.png" alt="Add To Event Featured Supplier"/></a>
        </ul>
        <ul className="copyright">
          <li>&copy; 4T Knox Secure. All rights reserved.</li>
          <li>
            Design: <a href="http://html5up.net">HTML5 UP</a>
          </li>
        </ul>
      </div>
    </section>
  );
}
